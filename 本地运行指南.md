# PaperScholar 本地运行完整指南

> 本项目为 AI 学术图表生成平台，包含 FastAPI 后端 + Next.js 前端 + PostgreSQL + Redis。

---

## 前置要求

- **Docker Desktop**（推荐，最简单）
  - Windows 用户必须安装并**启动** Docker Desktop
  - 下载：https://www.docker.com/products/docker-desktop/
- **Node.js 18+**（如果想原生运行前端进行开发）
- **Python 3.12+**（如果想原生运行后端）

当前仓库已为你准备好：
- `backend/.env`（已生成安全密钥 + 邀请码 `devinvite2026`）
- `docker-compose.yml` 已优化（自动执行数据库迁移）
- `start-local.ps1` 一键启动脚本

---

## 方式一：Docker 一键启动（推荐）

### 步骤

1. **启动 Docker Desktop**
   - 从开始菜单打开 Docker Desktop
   - 等待托盘小鲸鱼图标变成**绿色/正常状态**

2. **执行启动脚本（推荐）**
   ```powershell
   # 在项目根目录下
   .\start-local.ps1
   ```

   或手动执行：
   ```powershell
   docker compose down --remove-orphans
   docker compose up -d --build
   ```

3. **等待构建完成**（首次约 5~15 分钟）
   - 会自动拉取 postgres:16, redis:7, node:20, python:3.12
   - 后端启动时会自动运行 `alembic upgrade head` 数据库迁移

4. **访问服务**

   | 服务       | 地址                          | 说明                  |
   |------------|-------------------------------|-----------------------|
   | 前端 UI    | http://localhost:3000         | 主界面                |
   | 后端 API   | http://localhost:8000         | REST API              |
   | API 文档   | http://localhost:8000/docs    | Swagger UI            |
   | 健康检查   | http://localhost:8000/api/health | 确认后端是否就绪   |

5. **首次使用流程**

   - 打开 http://localhost:3000/register
   - **邀请码**：`devinvite2026`
   - 填写用户名、邮箱、密码注册
   - **第一个注册的用户自动成为管理员**
   - 登录后进入「设置 / API 配置」
   - 添加至少一个 **Chat 模型** 和一个 **Image 模型** 的 API Key
     - 支持 OpenAI 兼容（OpenRouter、中转）、Google Gemini、Anthropic Claude
   - 进入「图表生成」开始使用

---

## 方式二：混合开发模式（推荐长期开发）

适合你需要频繁修改代码的情况。

### 启动基础设施（DB + Redis）

```powershell
# 只启动数据库和 Redis
docker compose up -d db redis

# 查看状态
docker compose ps
```

### 启动后端（原生 Python）

```powershell
cd backend

# 创建虚拟环境（首次）
python -m venv .venv

# 激活（PowerShell）
.\.venv\Scripts\Activate.ps1

# 安装依赖（首次，注意 Python 版本兼容性）
pip install -r requirements.txt

# 确保 .env 存在（已为你创建）
# 如需原生连接本地 DB，把 DATABASE_URL 改成 localhost

# 运行迁移（确保 DB 已启动）
alembic --sqlalchemy-url "postgresql+asyncpg://paperscholar:paperscholar@localhost:5432/paperscholar" -c alembic.ini upgrade head

# 启动开发服务器（热重载）
uvicorn app.main:app --reload --port 8000
```

### 启动前端（原生 Node）

```powershell
cd frontend

# 安装依赖（首次）
npm install

# 启动开发服务器（热重载 + HMR）
npm run dev
```

前端会自动使用 `http://localhost:8000`（见 `src/lib/api.ts` 的 fallback）。

---

## 常见问题排查

### 1. Docker 连接失败 / daemon not running

```
failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine
```

**解决**：必须手动打开 Docker Desktop 应用，等待完全启动后再试。

### 2. 后端启动报错 "服务器未配置邀请码"

- 检查 `backend/.env` 中的 `INVITE_CODE` 是否存在且非空
- 重启后端容器：`docker compose restart backend`

### 3. 注册后无法添加 API Key 或生成失败

- 必须先在「API 配置」页面添加有效的 LLM API Key
- Image 模型必须支持图像生成（gpt-4o, gemini-2.0-flash-exp, claude-3-5-sonnet 等）
- 查看后端日志定位问题：
  ```powershell
  docker compose logs -f backend
  ```

### 4. 前端构建/启动很慢

- 首次 `docker compose up --build` 会重新 npm ci + pip install
- 后续修改代码后端有热重载（volume mount + uvicorn --reload）
- 前端如需热重载开发，建议使用「混合模式」原生启动前端

### 5. 数据库迁移失败

- 确认 `db` 容器健康：`docker compose ps`
- 手动在 backend 容器内执行：
  ```powershell
  docker compose exec backend alembic --sqlalchemy-url "postgresql+asyncpg://paperscholar:paperscholar@db:5432/paperscholar" -c alembic.ini upgrade head
  ```

### 6. 端口冲突

- 5432 (PG), 6379 (Redis), 8000 (Backend), 3000 (Frontend)
- 如被占用，修改 `docker-compose.yml` 中的 ports

---

## 停止服务

```powershell
# 停止并删除容器（数据保留在命名卷中）
docker compose down

# 彻底清理（包括数据库数据，慎用）
docker compose down -v
```

---

## 生产部署参考

项目还提供了：
- `docker-compose.deploy.yml`
- `nginx.conf`
- `server_setup.sh` / `deploy_setup.sh`

详见 `DEPLOYMENT_GUIDE.md` 和 `docs/deploy-guide.md`。

---

## 技术支持

- 后端日志：`docker compose logs -f backend`
- Celery（如启用）：`docker compose -f docker-compose.celery.yml logs -f`
- 健康检查接口：`/api/health`

有问题可查看 `backend/app/main.py` 启动日志和 `/api/health` 返回内容。
